# Copy to .env and fill in. .env is gitignored.
#
# The LLM judge (src/gpt_judge_evaluation*.py) calls GPT-5.4-mini. Set the key
# for whichever provider you use: OpenAI directly, or OpenRouter via the
# --openrouter flag.

OPENAI_API_KEY=
OPENROUTER_API_KEY=
